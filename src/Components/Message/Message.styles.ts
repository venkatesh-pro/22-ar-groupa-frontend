import styled from "styled-components";

const loadingContainer = styled.div`
  height: 100vh;
  width: 100vw;
`;

const loadingheader = styled.h1`
  text-align: center;
  font-weight: bolder;
`;

const styles = { loadingContainer, loadingheader };

export default styles;
