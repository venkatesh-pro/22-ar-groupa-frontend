import styled from "styled-components";

const Header = styled.h1`
  text-align: center;
  margin: 60px auto;
`;

const ErrorImage = styled.img`
  margin: 100px;
  height: 300px;
  align-items: center;
  justify-content: center;
`;
const styles = { ErrorImage, Header };

export default styles;
