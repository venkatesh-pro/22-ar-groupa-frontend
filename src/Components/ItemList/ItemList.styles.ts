import styled from "styled-components";

const itemListContainer = styled.div`
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  width: 100%;
`;

const styles = { itemListContainer };

export default styles;
