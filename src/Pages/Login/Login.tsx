import { Header } from "../../Components/Header/Header";
import s from "./Login.styles";

export const Login: React.FC = () => {
  return (
    <s.loginContainer>
      <Header />
      Login
    </s.loginContainer>
  );
};
